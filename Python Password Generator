import random
import string

// Prompt the user to enter a desired length for their password.
passwordLength = int(input("Enter password length: "))

// Include all possible letters, numbers, and special characters to be used for the password.
passwordChars = string.ascii_letters
passwordChars += string.digits
passwordChars += string.punctuation

// Create a password variable to be used.
password = ""

// Add a new random character for the password as many times as the user had input.
for i in range(length):
        generatedChar = random.choice(passwordChars)
        password += generatedChar

// Display the generated password.
print("Your password: ", password)
